package main

import (
	// "fmt"
	// "os"
	// "strconv"
	// "strings"
)

// func main()  {
//	/*
//		ep120: comparison operators
//	 */
//	fmt.Println("Hello")
//
//	 speed := 100
//	 fast := speed >= 80
//	 slow := speed < 20
//
//	 fmt.Printf("fast's type is %T\n", fast)
//	fmt.Printf("going fast? %t\n", fast)
//	 fmt.Printf("going slow? %t\n", slow)
//
//
//	 fmt.Printf("is it 100 mph? %t\n", speed !=100)
//
////	 ---------------------------------------------------------
//
//	fmt.Println(strings.Repeat("-", 25))
//
//	speedB := 150.5
//	faster := speedB > 100
//	fmt.Printf("is the other car going faster? %t\n", faster)
//
//	faster = speedB > float64(speed)
//	fmt.Println("is the other car going faster",faster)
	//fmt.Println(true > faster)

		/*
			logic and &&
	 	*/
		// speed :=100
		// fmt.Println("within limits?", speed >= 40 && speed <= 55)

		// /*
		// 	logic or ||
		//  */
		// color := "red"
		// fmt.Println("reddish color?", color == "red" || color == "dark red")

		/*
		logic not !=
		 */
		//var on bool
		//fmt.Println(on)
		//
		//i, f := 15, 5.0
		//fmt.Printf("i = %T\n and f= %T\n", i, f)
		//
		//f1 := int(f)
		//fmt.Printf("i = %d and f1 = %d\n", i, f1)

	/*
	if - statement
	 */
// 	const usage = `
// 	Feet to Meters
// ---------------
// this program converts feet into meters.

// Usage:
// feet [feetsToConvert]
// `

// 	if len(os.Args) < 2 {
// 		fmt.Println("please tell me a value in feet")
// 		fmt.Println(strings.TrimSpace(usage))
// 		return
// 	}

// 	arg := os.Args[1]
// 	feet, _ := strconv.ParseFloat(arg,64)
// 	meters := feet * 0.3048
// 	fmt.Printf("%g feet is %g meters. \n", feet, meters)



	/*
		validate single user
	*/



// }